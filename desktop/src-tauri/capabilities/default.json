{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Lele Desktop window",
  "windows": ["main", "splash"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-open",
    "opener:default"
  ]
}